{% extends 'base.html.twig' %}

    {% block title %}
        SnowTricks
    {% endblock %}

    {% block body %}

        <h1>Home Page<h1>

        {% for f in figures %}
            {{ dump(f) }}
        {% endfor %}

    {% endblock %}